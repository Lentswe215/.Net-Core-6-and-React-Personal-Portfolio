namespace LenMamPortfolio.Helpers
{
    public enum UserTypes { SuperAdmin = 1, SiteAdmin = 2 };
    public enum SkillTypes { Frontend = 1, Backend = 2, OS = 3, IDEs = 4, DataBase = 5 }
    public enum PageTypes { AboutUs = 1, HomePage = 2 }

}
